<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <%- include ('partials/script'); -%> <%- include ('partials/head'); -%>
  </head>
  <body>
    <%- include ('partials/navbar'); -%>

    <div class="details content">
      <h1><%= article.title %></h1>
      <div class="content">
        <p><%= article.article %></p>
      </div>
      <% if(user){if ( user.id == article.user_id){%>
      <form
        style="display: inline"
        method="POST"
        action="/art/article/<%= article.art_id %>"
      >
        <button
          class="btn btn-danger"
          onclick="return confirm('are you sure you want to delete this article?')"
        >
          Delete
        </button>
      </form>
      <a href="/art/modify/<%= article.art_id %>" class="btn btn-primary"
        >Modify
      </a>
      <% }} %>
    </div>
    <div class="container m-5 p-5"></div>

    <%- include ('partials/footer'); -%>
  </body>
  <!-- <script>
    const trashcan = document.querySelector("a.delete");
    trashcan.addEventListener("click", (e) => {
      const endpoint = `/article/${trashcan.dataset.doc}`;
      fetch(endpoint, {
        method: "DELETE",
      })
        .then((response) => response.json())
        .then((data) => (window.location.href = data.redirect))
        .catch((err) => console.log(err));
    });
  </script> -->
</html>
